# AutoDoc 智驾医生 — 项目介绍

> 修车前，先问 AI，让每一分钱都花在刀刃上。

**AutoDoc 智驾医生**是一款面向车主的 **AI 自助预诊断工具**：在去修车之前，车主用自然语言描述车辆症状，AI 通过多轮对话追问关键信息，给出可能的故障原因、维修方案、透明报价和"防被宰"提醒，让车主在走进修理厂之前心里有数。

本项目为"经开智造"AI 智能体大赛（武汉经开区）参赛作品，定位为**场景落地类**应用。

---

## 一、项目解决什么问题

中国汽车维修市场存在严重的信息不对称：

- 车主不懂车，故障灯一亮只能听修理厂怎么说；
- 部分维修机构利用信息差过度维修，小故障大修、模糊报价；
- 车主没有可靠的渠道提前了解"大概什么问题、大概要花多少钱"。

AutoDoc 的定位不是替代专业技师，而是 **"修车前的预诊断"**：让车主带着初步判断和价格预期进店，识别可疑建议，避免被宰。

---

## 二、目标用户

| 用户 | 场景 |
|------|------|
| 普通车主（核心） | 车辆出现异常，先去 AutoDoc 问一轮，心里有数再去修 |
| 二手车买家（次要） | 用 AI 快速评估车况与报价合理性，识别卖家话术中的坑 |

---

## 三、核心功能

### 1. 智能问诊引擎（核心）

- 多轮对话式问诊：AI 像专业技师一样追问车型、发生条件、持续时间、伴随症状等关键信息；
- 流式输出（SSE）：回复实时呈现，体验接近 ChatGPT；
- 结构化诊断报告：故障原因按概率排序、建议检查项目、维修方案、三种配件价格（原厂/品牌/副厂）、严重程度与可否继续行驶；
- **防被宰提醒**：揭露常见过度维修套路，标注可疑建议。

### 2. 结构化故障知识库

- 内置 **8 大汽车系统、42 条故障知识**（发动机、变速箱、底盘悬挂、制动、电气、空调、车身、转向）；
- 每条知识包含多维度口语化症状、可能原因、检查项、维修方案、价格区间与防被宰提醒；
- 对话时按症状做关键词匹配，把相关知识注入 Prompt，提升诊断的准确性和一致性。

### 3. 二手车车况快评

- 输入车辆信息 + 卖家描述，AI 输出综合评分（0–100）、合理价格区间、卖家描述可疑点、该车型常见坑点与购买建议。

### 4. 数据与账号体系

- 用户注册/登录（邮箱或用户名，JWT 会话，密码 bcrypt 加密）；
- 车辆管理（增删改查）、诊断历史（自动保存、搜索回顾）、个人中心；
- 管理后台（仅 admin 角色）：数据概览、知识库浏览与检索、使用统计、一键导入演示数据。

---

## 四、一个典型使用流程

1. 用户在首页输入症状："冷启动时发动机抖动明显，热车后好转"；
2. AI 追问："什么车型？持续多久了？故障灯亮了吗？最近保养过吗？"；
3. 用户补充："大众朗逸 2020 款 1.5L，一两周了，怠速转速 700–900 波动"；
4. AI 输出诊断报告：**节气门积碳可能性最高** → 清洗节气门 150–250 元（4S 店 300–500 元）→ ⚠️ 防被宰提醒"别被忽悠换整个进气歧管"；
5. 报告自动保存到历史，可随时回顾。

---

## 五、技术架构

| 层级 | 选型 |
|------|------|
| 前端 | Next.js 16（App Router）+ React 19 + Tailwind CSS 4 + shadcn/ui |
| 后端 | Next.js API Routes（全栈一体，17 个 REST 接口） |
| LLM | DeepSeek 原生 Responses API（SSE 流式输出） |
| 数据库 | PostgreSQL 16（自托管，Docker 卷持久化） |
| 认证 | 自研 JWT 会话（httpOnly Cookie + bcrypt） |
| 部署 | Docker Compose（自购云服务器）+ GitHub Actions CI |
| 样式 | 杂志编辑风（暖纸底 / 墨黑 / 编辑红，系统衬线字体） |

### 数据流

```
用户输入症状
   → 知识匹配引擎（症状 → 相关故障知识）
   → DeepSeek Responses API（系统提示 + 知识上下文 + 对话历史）
   → SSE 流式回复 / 结构化诊断报告
   → PostgreSQL 持久化（会话、消息、车辆、评估记录）
```

### 工程与安全特性

- 幂等数据库迁移脚本（`db/migrations/` + `npm run db:migrate`）；
- 认证与 LLM 接口内置速率限制（429 + Retry-After）；
- 数据按用户隔离，所有资源接口校验归属；管理后台有 admin 角色守卫；
- 生产环境强制 HTTPS（会话 Cookie Secure 标记）、JWT_SECRET 强度校验；
- 一键备份/恢复（`make backup` / `make restore`）；
- CI 自动跑 lint、类型检查、构建与 Docker 全链路冒烟测试；
- 依赖审计零漏洞，仓库开启 Secret Scanning + Push Protection。

---

## 六、部署

```bash
cp .env.example .env   # 填入 DeepSeek Key、数据库密码、JWT_SECRET、ADMIN_EMAILS
docker compose up -d --build
docker compose exec -T app node scripts/migrate.mjs
```

生产环境建议 Nginx 反向代理 + HTTPS（README 提供完整配置示例，SSE 需 `proxy_buffering off`）。

---

## 七、当前状态与路线图

**已完成**：

- 核心问诊、知识库、报告、车辆管理、历史、二手车评估、用户系统、管理后台全部可用；
- 全自托管改造完成（PostgreSQL + JWT，移除 Supabase）；
- Docker 化部署与 CI 就绪。

**规划中（Phase 7）**：

- 向量化知识检索（替代关键词匹配，提升召回准确率）；
- 诊断报告 JSON 结构化输出；
- 自动化测试补全；
- 多实例部署时的 Redis 共享限流。

---

## ⚠️ 免责声明

AutoDoc 提供的所有诊断结果、维修方案和价格信息**仅供参考**，不构成专业维修建议。具体故障原因与维修方案请以专业技师现场检查为准；价格因地区和渠道而异。
